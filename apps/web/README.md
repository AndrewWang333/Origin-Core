# @origin/web — Origin frontend (placeholder)

The Origin frontend lives in a separate Next.js workspace and is not part of this repository scaffold. This package exists as a placeholder so the monorepo references resolve cleanly.
